# Template only — Vite does NOT load this file. Copy it to .env.local and fill in.
# Both values are safe to expose in the client (find them in the Supabase
# dashboard → Project Settings → API). Use the "publishable" key
# (sb_publishable_…); the legacy "anon" key also works as a fallback.
VITE_SUPABASE_URL=https://vpwrjrvusrjunqvyxcnp.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
